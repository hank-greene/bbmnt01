
/opt/tomcat/conf

updates to server.xml

    <!-- original 8080 setings
    <Connector port="8080" protocol="HTTP/1.1"
               connectionTimeout="20000"
            redirectPort="8443" />
    -->
    <!-- server=" " removes server info from site scans -->
    <Connector port="8080" 
               protocol="HTTP/1.1"
               connectionTimeout="20000"
               redirectPort="8443"
               Server=" " />


